import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.speedpay.finance",
  appName: "speedpayapp",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
